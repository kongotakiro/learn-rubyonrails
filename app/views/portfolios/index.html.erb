<h1>Portfolio Items</h1>

<% @portfolio_items.each do |portfolios_item| %>
  <p><%= portfolios_item.title %></p>
  <p><%= portfolios_item.subtitle %></p>
  <p><%= portfolios_item.body %></p>
  <%= image_tag portfolios_item.thumb_image if !portfolios_item.thumb_image.nil? %>
<% end %>
